\ProvidesPackage{epstopdf-base}[2026/01/16 local stub for PNG-only builds]
% This project embeds only PNG figures in docs/auto_best_run.
% Some environments auto-load epstopdf-base; if it is missing or slow,
% this stub avoids blocking the PDF build.
\endinput
