[pytest]
# Scope default collection to tests/ so stray root-level scripts named
# like tests (e.g. established_test.py) are not imported during `pytest`.
# The Fortran cross-check suites live outside tests/ and are invoked by
# explicit path in CI (which overrides testpaths).
testpaths = tests
addopts = -q
